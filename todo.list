-gestion d'erreur: migrer vers une gestion d'erreur sur la sortie d'erreur non plus la sortie standard

-gestion du tilde dans les commandes

-check memory leaks for history : "memory still reachable"

-manage history when "rm /tmp/.history"

DONE:

- merge & delete integration-termcaps branch
-Boid