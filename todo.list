TODO:

$$$$ PROPOSITIONS $$$$
-faire un define pour e->tcaps.ws.ws_col qui n'est pas franchement tres clair


!?!?!?!?  MODIFIER FICHIER AUTEUR POUR LE 21SH  !?!?!?!?


- check memory leaks for history : "memory still reachable"

- gestion d'erreur

REDIRECTIONS

- parsing pour '\'

- ls |
pipe:

- ctrl+arrow left/right with 'word-word' has to move to the -

define __attribute__((unused)) in .h

- in ft_exec.c: delete waitpid and replace it with something who permit : ls -R / | grep string

- gestion de l'environnement via termios a chaque execution de commande

- manage afficha du prompt avec Ctrl+C lors d'un processus fils

- MAYBE: gerer differement l'insertion de caractere en milieu de ligne avec une cmd sur plusieurs lignes. On calcul le dernier caractere de chaque ligne qui va etre ecraser et on l'insert au debut de la la ligne suivante

- gerer le multi ligne lors du Ctrl + D

- segfault lors de Ctrl +k / Ctrl + p

- gestion des commandes avec pipe + redirections && separetrur ';'

- builtin in pipes

- (NO) builtin posix

- inhibitors --> ft_strsplit_quote

- OPERATEUR LOGIC

- Backquotes  "$> echo `pwd`"

- Globing  * ? [] {} (pas utiliser la fonction glob())

- Sub-shells ()

- Manipulation de fichiers (builtin read)

- Auto completion

- Variables locales (dans env)

- Historique ( avec les !! !)

- historique: up + down + new cmd

- tcaps_cut_paste : pas fonctionnel du tout

BAREME:

- add gestion de cmd sur plusieurs ligne (ex: "ls \-l \ -r")
- add couper/coller
- gestion fin `de cmd avec " ' \ { [ (
- add gestion du tilde
- add verification du type de fichier a executer lors de cmd avec une modif du PATH (ex: setenv PATH ~ && Desktop)
- RETESTER ECHO
- gestion des && et || (ex: ls something ||/&& ls)


Idea BONUS:

- recherche dans l'historique
- SEE table de hachage pour binaire

NEW FEATURES:

- Ctrl + a && Ctrl + e

CRASH TESTS :

- tester robustesse 'ls -R' avec une dizaine de redirections '>'

DONE:

- mvt ctrl + fleche
- add Ctrl + a || home && Ctrl + e || end
- integration multi ligne
- gerer l'affichage apres la cmd sur plusieurs ligne
- manage history when "rm /tmp/.history"
- gestion du tilde dans les commandes
- utilisation history : bug to fix: double up/down needed
- couper/coller avec ctrl+k / ctrl+p
- multi redirections sur fichiers / multi pipes / wombo-combo des 2
- POUR: ls -la | grep 21 | wc -l > test1 > test2
  		1. wc -l n'est pas execute (tester avec et sans '>')
		2. appelle test1 et test2 alors que les deux doivent etre geres ensemble dans redir_fill_output
- segault: Ctrl + arrow up/down && delete jusqu'au prompt et plus encore
- block ctrl + up on prompt (tcaps)
- manage last history : up down down : HISTORY IS NOW FULLY WORKING
- ctrl + l (=clear) needed to clean e->line and TCAPS value
- HISTORY : gerer movement + last history (based on csh)